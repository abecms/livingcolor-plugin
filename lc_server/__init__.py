"""LivingColor Server — delivery orchestration host.

The server owns readiness, work orders, execution graphs, gates, events,
and agent runtime integration. The desktop app is Mission Control only and
consumes this layer through HTTP APIs.
"""
