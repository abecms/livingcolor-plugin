---
status: failed
workflow_frt_status: failed
shadow_mode: false
created_pr: ""
sandbox_pr_url: ""
sandbox_jira_ticket: ""
hermes_version: "0.17.0"
hermes_plugin_enabled: true
hermes_gateway_ok: true
hermes_bootstrap: passed
setup: failed
settings: passed
integrations: blocked
readiness: blocked
gates: blocked
delivery: blocked
jira_writeback: blocked
github_publication: blocked
sprint_report: blocked
stripe: blocked
credentials:
  OPENROUTER_API_KEY: missing
  JIRA_URL: missing
  JIRA_USERNAME: missing
  JIRA_API_TOKEN: missing
  GITHUB_TOKEN: missing
  GH_TOKEN: missing
  STRIPE_SECRET_KEY: missing
  GITLAB_PERSONAL_ACCESS_TOKEN: missing
  LIVINGCOLOR_TEST_PROJECT_KEY: missing
  LIVINGCOLOR_TEST_GITHUB_REPO: missing
top_findings:
  - id: F1
    classification: setup_gap
    severity: critical
    summary: "Cursor Cloud Automation secrets not injected into agent VM environment; Jira/GitHub/Stripe integrations blocked."
  - id: F2
    classification: bug
    severity: medium
    summary: "PUT /project-config returned 400 without X-LC-Project-Key header while GET succeeded; fixed fallback to default project key."
  - id: F3
    classification: flaky test
    severity: low
    summary: "Vitest daily-analysis timestamp tests expect 2:00 PM but cloud VM TZ yields 12:00 PM."
---

# LivingColor TVP Cloud FRT — 2026-06-20-2046

## Executive Summary

Hermes Agent v0.17.0 bootstrapped successfully with the LivingColor plugin enabled and the delivery API mounted at `http://127.0.0.1:9119/api/plugins/livingcolor/delivery/overview` (HTTP 200).

**Outcome: `failed`.** All sandbox integration credentials (`JIRA_*`, `GITHUB_TOKEN`, `STRIPE_SECRET_KEY`, `OPENROUTER_API_KEY`) were **missing** from the Cloud Agent process environment. Workflow phases C–H could not execute against live Jira TVP or GitHub `abecms/tv5mondeplus-front`. `setup-automation` returned `prerequisites_missing: [jira_mcp, github_mcp]`.

Self-healing: added `scripts/cloud-preflight.sh` for early credential/Hermes gate checks; fixed `PUT /project-config` to fall back to the loaded default project key when no `X-LC-Project-Key` header is present.

**Action required before next run:** Configure Cursor **Automation** secrets per `docs/cursor-cloud/bootstrap.md` (IDE MCP connections do not populate the cloud VM environment).

## Workflow FRT Results (phase A–H)

| Phase | Step | Result | Evidence |
|-------|------|--------|----------|
| A | Hermes + plugin mount | **pass** | `hermes --version` → 0.17.0; plugin enabled; `GET .../delivery/overview` → 200 |
| B | setup-automation + TVP mapping | **fail** | `POST .../projects/TVP/setup-automation` → 400 `prerequisites_missing: [jira_mcp, github_mcp]` |
| B | project-config (TVP mapping) | **pass** | `GET .../project-config` → 200; mapping at `~/.hermes/livingcolor/project_mapping.yaml` |
| C | Jira / GitHub / Stripe live | **blocked** | `jira-projects` → 400 `jira_mcp_not_configured`; `vcs-repos` → 400 `github_mcp_not_configured`; env vars missing |
| D | Readiness scan + promote | **blocked** | Cannot scan Jira without MCP credentials |
| E | Human gates | **blocked** | No work order created |
| F | Analyst → publisher | **blocked** | No sandbox writes |
| G | Sprint report | **blocked** | `POST .../sprint/report` → skipped `no_active_sprint` (no prior workflow) |
| H | Stripe billing | **blocked** | `STRIPE_SECRET_KEY` not in environment |

## Hermes Bootstrap Result

| Check | Status |
|-------|--------|
| Hermes CLI | OK — v0.17.0 |
| Plugin installed | OK — `~/.hermes/plugins/livingcolor` |
| Plugin enabled | OK |
| Dashboard / API | OK — port 9119, session token `cloud-agent-session-token` |
| Overview endpoint | HTTP 200 |
| Shadow mode | Not set (correct) |
| Heuristic backends | `LIVINGCOLOR_*_BACKEND=heuristic` active |

Bootstrap command: `./scripts/cloud-bootstrap.sh`

## Environment And Context

- **Trigger commit:** `0994b182` — feat: auto-provision Hermes MCP from cloud env secrets (#7)
- **Branch under test:** `feature/livingcolor-workflow-frt-4369`
- **Heal branch:** `cursor/livingcolor-cloud-heal-frt-4369`
- **Runtime:** `LIVINGCOLOR_SYNC_ORCHESTRATOR=1`, heuristic analyst/planner/developer/publisher/reporter backends
- **Prior successful run:** `2026-06-20-1907` (partial — core workflow OK when credentials were present)

## Integration Result (Jira + GitHub live sandbox)

| Integration | Status | Notes |
|-------------|--------|-------|
| Jira TVP | **blocked** | `JIRA_URL`, `JIRA_USERNAME`, `JIRA_API_TOKEN` missing from env |
| GitHub `abecms/tv5mondeplus-front` | **blocked** | `GITHUB_TOKEN` missing from env |
| Stripe test mode | **blocked** | `STRIPE_SECRET_KEY` missing from env |
| MCP auto-provision | **skipped** | `mcp_env_bootstrap` reports all credentials missing |

## Gate Validation Result

Not executed — no work order promoted.

## Jira Writeback Result

Not executed — Jira MCP not configured.

## GitHub Publication Result

Not executed — GitHub MCP not configured.

## Sprint Report Result

`POST /sprint/report` returned `status: skipped, reason: no_active_sprint` (expected without prior sprint workflow).

## Stripe Billing Result

Not executed — Stripe secret not in environment.

## Secondary Test Results

| Suite | Result |
|-------|--------|
| `tests/test_e2e_smoke.py` | 1 passed |
| `tests/delivery_runtime/test_orchestration_phase2.py` | 4 passed |
| `tests/delivery_runtime/test_delivery_api.py` | 13 passed, 3 failed (promote status async, sprint capacity unrelated to credentials) |
| `tests/lc_server/test_stripe_billing.py` | 2 passed |
| `ui` vitest | 157 passed, 2 failed (timezone: expected 2:00 PM, got 12:00 PM UTC) |
| `ui` build | passed |
| `python3 -m lc_server warm-skills-cache` | passed |

**Ignored pytest suites** (`pytest.ini`):

- `tests/delivery_runtime/test_bn_shadow_evaluation.py`
- `tests/delivery_runtime/test_gate1_phase25_validation.py`
- `tests/delivery_runtime/test_live_evaluation.py`
- `tests/delivery_runtime/test_shadow_evaluation.py`

## Findings

| ID | Classification | Severity | Summary |
|----|----------------|----------|---------|
| F1 | setup_gap | critical | Automation secrets not injected; blocks all live sandbox workflow phases |
| F2 | bug | medium | `PUT /project-config` 400 without project header — fixed in heal branch |
| F3 | flaky test | low | Vitest TZ-dependent daily-analysis timestamp assertions |

## Self-Healing Changes

1. **`delivery_runtime/api/routes.py`** — `_require_project_key` falls back to `load_delivery_automation_config().project_key` when no request/body key (aligns PUT with GET).
2. **`scripts/cloud-preflight.sh`** — Early Hermes + credential gate before workflow FRT.
3. **`docs/cursor-cloud/bootstrap.md`** — Document preflight script.

## Next Agent Instructions

1. **Configure Automation secrets** on the Cursor Cloud automation that triggers this run:
   - `JIRA_URL`, `JIRA_USERNAME`, `JIRA_API_TOKEN`
   - `GITHUB_TOKEN` (and `GH_TOKEN` for heal PR creation)
   - `STRIPE_SECRET_KEY` (`sk_test_*`)
   - Optional: `OPENROUTER_API_KEY`, `STRIPE_TEST_CUSTOMER_ID`
2. Re-run `./scripts/cloud-bootstrap.sh` then `./scripts/cloud-preflight.sh` — preflight must exit 0.
3. Execute workflow FRT phases B–H via Hermes API with `X-Hermes-Session-Token` and `X-LC-Project-Key: TVP`.
4. Create a dedicated sandbox Jira ticket (or reuse TVP-10+) for promote/gates/delivery.

## Appendix

### API calls recorded

```bash
# Phase B
POST /api/plugins/livingcolor/delivery/projects/TVP/setup-automation → 400 prerequisites_missing

# Phase B settings
GET /api/plugins/livingcolor/delivery/project-config → 200 (TVP, preprod, GitHub)

# Phase C
GET .../projects/TVP/jira-projects → 400 jira_mcp_not_configured
GET .../projects/TVP/vcs-repos → 400 github_mcp_not_configured

# Phase G
POST .../sprint/report → 200 skipped no_active_sprint
```

### Credential injection note

The automation prompt listed expected credential variable names, but `printenv` on the Cloud Agent VM showed none of `JIRA_*`, `GITHUB_TOKEN`, `STRIPE_SECRET_KEY`, or `OPENROUTER_API_KEY` were set. This matches the prior run `2026-06-20-2022` (frt-5fec). Secrets must be configured in the **Automation** settings UI, not only as IDE MCP connections.
