---
status: partial
workflow_frt_status: partial
shadow_mode: false
created_pr: ""
sandbox_pr_url: "https://github.com/abecms/tv5mondeplus-front/pull/5"
sandbox_jira_ticket: "TVP-10"
hermes_version: "0.17.0"
hermes_plugin_enabled: true
hermes_gateway_ok: true
hermes_bootstrap: passed
setup: passed
settings: passed
integrations: passed
readiness: passed
gates: passed
delivery: passed
jira_writeback: passed
github_publication: passed
sprint_report: blocked
stripe: passed
credentials:
  OPENROUTER_API_KEY: configured
  JIRA_URL: configured
  JIRA_USERNAME: configured
  JIRA_API_TOKEN: configured
  GITHUB_TOKEN: configured
  GH_TOKEN: configured
  STRIPE_SECRET_KEY: configured
  GITLAB_PERSONAL_ACCESS_TOKEN: configured
top_findings:
  - id: F1
    classification: bug
    severity: high
    summary: "cloud-workflow-frt.sh used read -r on two-line api output; JSON body on line 2 was dropped — fixed with api_call helper."
  - id: F2
    classification: integration_gap
    severity: medium
    summary: "Hermes messaging unavailable in cloud; sprint report composes and bills but delivery fails (invoice still returned)."
  - id: F3
    classification: regression
    severity: low
    summary: "test_delivery_api promote/gate tests expect awaiting_gate at promote time; live sync orchestrator advances to intake — pre-existing."
---

# LivingColor TVP Cloud FRT — 2026-06-23-0643

## Executive Summary

Hermes Agent v0.17.0 with LivingColor plugin v0.3.18 was bootstrapped on port 9119. The full TVP delivery workflow was exercised end-to-end via `scripts/cloud-workflow-frt.py` against live sandbox integrations (Jira TVP, GitHub `abecms/tv5mondeplus-front`, Stripe test mode).

**Outcome: `partial`.** Core delivery through human gates, heuristic analyst/planner/developer/publisher, GitHub PR publication on **TVP-10** (PR #5 → `preprod`), and Jira comment writeback succeeded. Sprint report message delivery remains blocked (no Hermes messaging home channel); Stripe test draft invoice was created (`in_1TlO1m1rbdMPTLCddQfyiin2`). Self-healing fix applied for `cloud-workflow-frt.sh` HTTP/body parsing.

Trigger: push to `main` (`53b165b` — fix null `estimatedDays` on sprint inbox tickets).

## Workflow FRT Results (phase A–H)

| Phase | Step | Result | Evidence |
|-------|------|--------|----------|
| A | Hermes + plugin mount | **pass** | `hermes --version` → 0.17.0; plugin enabled; overview → 200 |
| B | setup-automation + TVP mapping | **pass** | `POST .../projects/TVP/setup-automation` → 200 |
| C | Jira / GitHub / Stripe live | **pass** | Jira TVP 200; GitHub repo 200; Stripe balance `livemode=false` |
| D | Readiness scan + promote | **pass** | TVP-10 (RD-4) → WO-1; sprint pinned via selection |
| E | Human gates | **pass** | G-1..G-4 approved (`cloud-agent:test`) |
| F | Analyst → publisher | **pass** | PR https://github.com/abecms/tv5mondeplus-front/pull/5 (`feature/TVP-10`) |
| G | Sprint report | **blocked** | Message composed; `status=failed` — no Hermes messaging home channel |
| H | Stripe billing | **pass** | Draft invoice `in_1TlO1m1rbdMPTLCddQfyiin2` via heuristic billing |

## Hermes Bootstrap Result

| Check | Status |
|-------|--------|
| `hermes` CLI | v0.17.0 |
| Plugin sync | `~/.hermes/plugins/livingcolor` |
| Plugin enabled | yes (v0.3.18) |
| API host | `http://127.0.0.1:9119` |
| Overview endpoint | `/api/plugins/livingcolor/delivery/overview` → 200 |
| `uvx` (mcp-atlassian) | installed |
| Jira MCP warmup | 53 tools |

## Environment And Context

- Automation run: `4a7f1afd`
- Heal branch: `cursor/livingcolor-cloud-heal-4a7f1afd`
- Runtime: `LIVINGCOLOR_SYNC_ORCHESTRATOR=1`, heuristic backends (analyst/planner/developer/publisher/sprint-reporter/billing)
- `LIVINGCOLOR_SHADOW_MODE`: unset (false)
- Credentials: provisioned via `cloud-start.sh` stdin from automation prompt

## Integration Result (Jira + GitHub live sandbox)

| Integration | Status | Notes |
|-------------|--------|-------|
| Jira TVP | available + writable | TVP-10; comment writeback on publication |
| GitHub sandbox | available + writable | PR #5, branch `feature/TVP-10` → `preprod` |
| Stripe test | available + writable | Customer `cus_Uktoa0hxJCOlIU`; draft invoice created |
| MCP Jira | connected | mcp-atlassian via uvx |
| MCP GitHub | connected | server-github |

## Gate Validation Result

Four gates required explicit API approval (`analysis_plan`, `code_review`, `merge_request_review`, `jira_update`). WO-1 final status **completed**. Workflow paused at each gate until `POST /gates/{id}/approve`.

## Jira Writeback Result

- Comment posted on TVP-10: "Delivery published for TVP-10" with PR URL (no `shadow_mode` skip)
- Multiple publication comments from prior runs also visible

## GitHub Publication Result

- Branch: `feature/TVP-10`
- PR: https://github.com/abecms/tv5mondeplus-front/pull/5
- Target: `preprod`

## Sprint Report Result

- API: `POST /sprint/report?force=true` → 200
- Heuristic message composed (French)
- Messaging: **blocked** — `No enabled messaging platform with a home channel found in Hermes`
- Billing attached: `invoiceId=in_1TlO1m1rbdMPTLCddQfyiin2`, `billingStatus=draft_created`

## Stripe Billing Result

- Test key validated (`livemode=false`)
- Invoice: `in_1TlO1m1rbdMPTLCddQfyiin2` (draft, heuristic billing)
- Heuristic billing backend (`LIVINGCOLOR_SPRINT_BILLING_BACKEND=heuristic`)

## Secondary Test Results

| Suite | Result |
|-------|--------|
| `tests/test_e2e_smoke.py` | 1 passed |
| `tests/delivery_runtime/test_orchestration_phase2.py` | 4 passed |
| `tests/delivery_runtime/test_delivery_api.py` | 2 failed, 14 passed (pre-existing) |
| `tests/lc_server/test_stripe_billing.py` | 2 passed |
| `ui` vitest | 2 failed, 157 passed (timezone/locale) |
| `ui` build | passed |
| `lc_server warm-skills-cache` | passed |

**Ignored pytest suites** (`pytest.ini`):

- `tests/delivery_runtime/test_bn_shadow_evaluation.py`
- `tests/delivery_runtime/test_gate1_phase25_validation.py`
- `tests/delivery_runtime/test_live_evaluation.py`
- `tests/delivery_runtime/test_shadow_evaluation.py`

## Findings

| ID | Class | Severity | Summary |
|----|-------|----------|---------|
| F1 | bug | high | Shell FRT script dropped JSON body on line 2 when parsing `api` output — fixed with `api_call` helper |
| F2 | integration_gap | medium | Hermes messaging home channel absent in cloud VM |
| F3 | regression | low | `test_delivery_api` promote/gate expectations vs sync orchestrator intake status |

## Self-Healing Changes

1. `scripts/cloud-workflow-frt.sh` — `api_call` helper splits HTTP code and JSON body correctly
2. `scripts/cloud-start.sh` — log pointer to preferred Python FRT runner
3. `docs/cloud-agent-runs/2026-06-23-0643-result.md` — this report

## Next Agent Instructions

1. Configure Hermes messaging home channel in cloud if sprint report delivery must pass (Phase G).
2. Prefer `python3 scripts/cloud-workflow-frt.py` over the shell script for FRT.
3. Consider aligning `test_delivery_api` promote assertions with sync-orchestrator behavior.
4. Use a fresh sandbox Jira ticket per run (TVP-14+ or newly created) to avoid reusing PR #5.

## Appendix

- Evidence: `/tmp/lc-frt-evidence.jsonl`, `/tmp/lc-frt-summary.json`
- Hermes log: `/tmp/hermes-dashboard.log`
- Stripe customer: `cus_Uktoa0hxJCOlIU` (created this run)
