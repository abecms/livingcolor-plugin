---
status: partial
workflow_frt_status: partial
shadow_mode: false
created_pr: pending
sandbox_pr_url: https://github.com/abecms/tv5mondeplus-front/pull/3
sandbox_jira_ticket: TVP-17
setup: pass
settings: pass
integrations: pass
readiness: pass
gates: pass
delivery: pass
jira_writeback: partial
github_publication: pass
sprint_report: blocked
stripe: pass
credentials:
  OPENROUTER_API_KEY: configured
  JIRA_URL: configured
  JIRA_USERNAME: configured
  JIRA_API_TOKEN: configured
  GITHUB_TOKEN: configured
  STRIPE_SECRET_KEY: configured
top_findings:
  - id: F-001
    class: integration_gap
    severity: high
    title: mcp-atlassian estimate read uses wrong argument envelope during gate-1 writeback
  - id: F-002
    class: setup_gap
    severity: medium
    title: Sprint report skipped when no active sprint window is configured
  - id: F-003
    class: missing test
    severity: low
    title: Cloud FRT script should assert Jira comment presence after jira_update gate
---

# LivingColor TVP Cloud Workflow FRT — 2026-06-20

## Executive Summary

The TVP delivery workflow was exercised end-to-end against live sandbox integrations (Jira TVP, GitHub `abecms/tv5mondeplus-front`, Stripe test mode) using heuristic analyst/planner/developer/publisher backends and `LIVINGCOLOR_SYNC_ORCHESTRATOR=1`. Shadow mode remained disabled throughout.

Core delivery path succeeded on the final run (`20260620-180056`): readiness promote, four human gates (simulated via API), heuristic development, GitHub PR publication, and work-order completion. GitHub write access and Stripe test invoicing were verified. Jira estimate writeback failed due to an MCP argument mismatch with `mcp-atlassian`; Jira delivery comment writeback was observed on an earlier run (`TVP-16`) but not re-verified on the final ticket (`TVP-17`). Sprint report publication was skipped because no active sprint window exists.

Self-healing changes were applied on branch `cursor/livingcolor-cloud-heal-20260620-173540` and are ready for plugin heal PR.

## Workflow FRT Results (phase A–H)

| Phase | Description | Result | Evidence |
|-------|-------------|--------|----------|
| A | Bootstrap / overview | pass | HTTP 200 via FastAPI TestClient |
| B | Project setup + automation | pass | `POST .../projects/TVP/setup-automation` → 200 |
| C | Live integrations | pass | Jira TVP ticket created; GitHub repo push=true; Stripe customer list 200 |
| D | Readiness → promote | pass | Sandbox ticket promoted; WO `awaiting_gate` with Gate 1 |
| E | Human gates | pass | 4 gates approved (`cloud-agent:test`); WO reached `completed` |
| F | Delivery execution | pass | Branch `feature/TVP-17`; PR https://github.com/abecms/tv5mondeplus-front/pull/3 |
| G | Sprint report | blocked | `no_active_sprint` (HTTP 200, skipped) |
| H | Stripe billing | pass | Test invoice `in_1TkTCk1rbdMPTLCdZSANhHo8` |

## Environment And Context

- **Runtime mode:** `LIVINGCOLOR_SYNC_ORCHESTRATOR=1`, heuristic analyst/planner/developer/publisher backends
- **API mode:** FastAPI TestClient mounting `dashboard/plugin_api.py` (no live Hermes gateway in cloud VM)
- **LivingColor home:** `~/.hermes/livingcolor/`
- **Ignored pytest suites:** `test_bn_shadow_evaluation.py`, `test_gate1_phase25_validation.py`, `test_live_evaluation.py`, `test_shadow_evaluation.py`

## Integration Result (Jira + GitHub live sandbox)

| Integration | Status | Notes |
|-------------|--------|-------|
| Jira TVP | available | Tickets created (`TVP-13`…`TVP-17`); REST API 200 |
| GitHub `abecms/tv5mondeplus-front` | available | Admin/push permissions; PRs #2 and #3 created |
| Stripe test mode | available | Customer `cus_UjXmIWTysd2fe0`; draft invoice created |

## Gate Validation Result

All four gates paused orchestration and advanced only after explicit `POST /gates/{id}/approve` with `approvedBy: cloud-agent:test`:

1. `analysis_plan` → `code_review`
2. `code_review` → `mr_draft`
3. `merge_request_review` → `jira_review`
4. `jira_update` → `completed`

No auto-skip observed between gates.

## Jira Writeback Result

| Action | Result |
|--------|--------|
| Original estimate (Gate 1) | failed — MCP `get_issue` argument validation (`issue_key` vs `issueKey`) |
| Delivery comment (Gate 4) | partial — confirmed on `TVP-16`; not present on final `TVP-17` automated run |
| Status transition | not applied (ticket remained in Backlog on inspected runs) |

No `shadow_mode` skip reasons were observed.

## GitHub Publication Result

| Field | Value |
|-------|-------|
| Repository | `abecms/tv5mondeplus-front` |
| Target branch | `preprod` |
| Delivery branch | `feature/TVP-17` |
| PR URL | https://github.com/abecms/tv5mondeplus-front/pull/3 |
| Publisher backend | heuristic (git push + GitHub REST) |

## Sprint Report Result

`POST /delivery/sprint/report` returned HTTP 200 with `status: skipped`, `reason: no_active_sprint`. Hermes messaging handoff not exercised.

## Stripe Billing Result

Stripe test-mode invoice creation succeeded (`in_1TkTCk1rbdMPTLCdZSANhHo8`).

## Secondary Test Results

| Suite | Result |
|-------|--------|
| `tests/test_e2e_smoke.py` | pass |
| `tests/delivery_runtime/test_orchestration_phase2.py` | pass |
| `tests/delivery_runtime/test_git_branch.py` | pass (incl. new TVP preprod candidate test) |
| `tests/delivery_runtime/test_delivery_api.py` | 4 failures (Hermes TestClient harness / isolation; pre-existing pattern in cloud env) |
| `ui` vitest/build | not run (out of scope for heal PR) |

## Findings

| ID | Class | Severity | Summary |
|----|-------|----------|---------|
| F-001 | integration_gap | high | Gate-1 Jira estimate writeback fails against `mcp-atlassian` tool schema |
| F-002 | setup_gap | medium | Sprint report requires active sprint configuration before publish path is meaningful |
| F-003 | bug | high | Managed checkout shallow clone ignored `integration_branch: preprod` (fixed) |
| F-004 | bug | critical | Heuristic publisher lacked `targetRepo` in publication context (fixed) |
| F-005 | bug | high | `github_token_from_config` ignored `GITHUB_PERSONAL_ACCESS_TOKEN` (fixed) |
| F-006 | improvement | medium | Cloud runs need heuristic analyst/planner/publisher backends (added) |

## Self-Healing Changes

Branch: `cursor/livingcolor-cloud-heal-20260620-173540`

- Heuristic backends: `LIVINGCOLOR_ANALYST_BACKEND`, `LIVINGCOLOR_PLANNER_BACKEND`, `LIVINGCOLOR_PUBLISHER_BACKEND`
- `RepoAwarePlanner` + `HeuristicPublisherAgent` (git push + GitHub REST PR creation)
- TVP `preprod` integration branch honored for managed clones and delivery branches
- GitHub token resolution from MCP env aliases
- Gate approve API accepts `approvedBy`
- Cloud scripts: `scripts/setup_cloud_env.sh`, `scripts/run_cloud_workflow_frt.py`

## Next Agent Instructions

1. Merge heal PR and re-run FRT on a clean VM with Hermes gateway optional.
2. Fix `McpJiraEstimateInvoker` / delivery invoker to connect MCP before gate-1 writeback in TestClient mode, or add REST fallback for cloud validation.
3. Seed an active TVP sprint window before asserting sprint report publish.
4. Add regression test for publication context `targetRepo` propagation.

## Appendix

- FRT JSON artifacts: `docs/cloud-agent-runs/20260620-180056-frt.json`
- Earlier partial runs: `20260620-173540-frt.json`, `20260620-175219-frt.json`
- Sandbox PRs created during validation: #2 (`TVP-16`), #3 (`TVP-17`)
