---
status: failed
workflow_frt_status: failed
shadow_mode: false
created_pr: ""
sandbox_pr_url: ""
sandbox_jira_ticket: ""
hermes_version: "0.17.0"
hermes_plugin_enabled: true
hermes_gateway_ok: true
hermes_bootstrap: passed
setup: failed
settings: passed
integrations: blocked
readiness: blocked
gates: blocked
delivery: blocked
jira_writeback: blocked
github_publication: blocked
sprint_report: blocked
stripe: blocked
credentials:
  OPENROUTER_API_KEY: missing
  JIRA_URL: missing
  JIRA_USERNAME: missing
  JIRA_API_TOKEN: missing
  GITHUB_TOKEN: missing
  GH_TOKEN: missing
  STRIPE_SECRET_KEY: missing
  GITLAB_PERSONAL_ACCESS_TOKEN: missing
  LIVINGCOLOR_TEST_PROJECT_KEY: missing
  LIVINGCOLOR_TEST_GITHUB_REPO: missing
top_findings:
  - id: F1
    classification: setup_gap
    severity: critical
    summary: "Cursor Cloud Automation secrets not injected into agent VM; Jira/GitHub/Stripe integrations blocked."
  - id: F2
    classification: bug
    severity: medium
    summary: "PUT /project-config returned 400 without X-LC-Project-Key when body omitted projectKey; restored default-key fallback."
  - id: F3
    classification: setup_gap
    severity: medium
    summary: "cloud-preflight.sh and cloud-workflow-frt.sh missing from main; restored for early gate and scripted FRT."
  - id: F4
    classification: flaky test
    severity: low
    summary: "Vitest daily-analysis timestamp tests expect 2:00 PM but cloud VM TZ yields 12:00 PM."
---

# LivingColor TVP Cloud FRT — 2026-06-22-1510

## Executive Summary

Hermes Agent v0.17.0 bootstrapped successfully with the LivingColor plugin enabled and the delivery API mounted at `http://127.0.0.1:9119/api/plugins/livingcolor/delivery/overview` (HTTP 200).

**Outcome: `failed`.** All sandbox integration credentials (`JIRA_*`, `GITHUB_TOKEN`, `STRIPE_SECRET_KEY`) were **missing** from the Cloud Agent process environment. Workflow phases C–H could not execute against live Jira TVP or GitHub `abecms/tv5mondeplus-front`. `setup-automation` returned `prerequisites_missing: [jira_mcp, github_mcp]`.

Self-healing on `livingcolor-plugin`: restored `scripts/cloud-preflight.sh`, added `scripts/cloud-workflow-frt.sh`, fixed `PUT /project-config` default project-key fallback in `_require_project_key`.

**Action required before next run:** Configure Cursor **Automation** secrets per `docs/cursor-cloud/bootstrap.md`. IDE MCP connections do not populate the cloud VM environment.

Trigger commit on `main`: `882af9b` — fix: skip effort estimation for tickets needing clarification.

## Workflow FRT Results (phase A–H)

| Phase | Step | Result | Evidence |
|-------|------|--------|----------|
| A | Hermes + plugin mount | **pass** | `hermes --version` → 0.17.0; plugin enabled; `GET .../delivery/overview` → 200 |
| B | setup-automation | **fail** | `POST .../projects/TVP/setup-automation` → 400 `prerequisites_missing: [jira_mcp, github_mcp]` |
| B | project-config (TVP mapping) | **pass** | `PUT/GET .../project-config` with `X-LC-Project-Key: TVP` → 200; mapping at `~/.hermes/livingcolor/project_mapping.yaml` |
| C | Jira / GitHub / Stripe live | **blocked** | `jira-projects` → 400 `jira_mcp_not_configured`; `vcs-repos` → 400 `github_mcp_not_configured`; env vars missing |
| D | Readiness scan + promote | **blocked** | `POST .../readiness/scan` → 503 `Jira MCP is not configured` |
| E | Human gates | **blocked** | No work order created |
| F | Analyst → publisher | **blocked** | No sandbox writes |
| G | Sprint report | **blocked** | `POST .../sprint/report` → `skipped` / `no_active_sprint` |
| H | Stripe billing | **blocked** | `STRIPE_SECRET_KEY` not in environment |

## Hermes Bootstrap Result

| Check | Status |
|-------|--------|
| Hermes CLI | OK — v0.17.0 (2026.6.19) |
| Plugin installed | OK — `~/.hermes/plugins/livingcolor` |
| Plugin enabled | OK |
| API host | `http://127.0.0.1:9119` |
| Overview endpoint | `/api/plugins/livingcolor/delivery/overview` → 200 |
| MCP SDK | `pip install mcp` (installed with hermes-agent) |
| Dashboard | `hermes dashboard --skip-build --no-open --port 9119` |

## Environment And Context

- Branch under test: `feature/livingcolor-workflow-frt-f0c9` (includes `882af9b` effort-estimation fix)
- Heal branch: `cursor/livingcolor-cloud-heal-frt-f0c9`
- Runtime: `LIVINGCOLOR_SYNC_ORCHESTRATOR=1`, heuristic analyst/planner/developer/publisher/sprint-reporter backends
- `LIVINGCOLOR_SHADOW_MODE`: unset (false)
- Automation trigger: push to `main` by Tamsi (`882af9b`)

## Integration Result (Jira + GitHub live sandbox)

| Integration | Status | Notes |
|-------------|--------|-------|
| Jira TVP | **blocked** | `JIRA_URL`, `JIRA_USERNAME`, `JIRA_API_TOKEN` missing from env |
| GitHub `abecms/tv5mondeplus-front` | **blocked** | `GITHUB_TOKEN` / `GH_TOKEN` missing |
| Stripe test mode | **blocked** | `STRIPE_SECRET_KEY` missing |
| MCP auto-provision | **skipped** | No credentials to write into `~/.hermes/config.yaml` |

## Gate Validation Result

Not executed — no work order promoted without Jira MCP.

## Jira Writeback Result

Not executed — blocked by missing Jira credentials.

## GitHub Publication Result

Not executed — blocked by missing GitHub token.

## Sprint Report Result

`POST /sprint/report` returned `status: skipped`, `reason: no_active_sprint` (no prior workflow data).

## Stripe Billing Result

Not executed — `STRIPE_SECRET_KEY` missing.

## Secondary Test Results

| Suite | Result |
|-------|--------|
| `tests/test_e2e_smoke.py` | 1 passed |
| `tests/delivery_runtime/test_orchestration_phase2.py` | 4 passed |
| `tests/delivery_runtime/test_delivery_api.py` | 14 passed, 2 failed (pre-existing: promote status `intake` vs `awaiting_gate`; gate approve) |
| `tests/lc_server/test_stripe_billing.py` | 2 passed |
| `ui` vitest | 157 passed, 2 failed (TZ: 12:00 PM vs 2:00 PM expected) |
| `ui` build | passed |
| `python3 -m lc_server warm-skills-cache` | passed |

**Ignored pytest suites** (from `pytest.ini`):

- `tests/delivery_runtime/test_bn_shadow_evaluation.py`
- `tests/delivery_runtime/test_gate1_phase25_validation.py`
- `tests/delivery_runtime/test_live_evaluation.py`
- `tests/delivery_runtime/test_shadow_evaluation.py`

## Findings

| ID | Classification | Severity | Summary |
|----|----------------|----------|---------|
| F1 | setup_gap | critical | Automation secrets not injected; blocks all live sandbox phases |
| F2 | bug | medium | `PUT /project-config` 400 without header/body project key — fixed fallback |
| F3 | setup_gap | medium | Missing `cloud-preflight.sh` / `cloud-workflow-frt.sh` — restored |
| F4 | flaky test | low | Vitest TZ-dependent daily-analysis caption |

## Self-Healing Changes

1. **`delivery_runtime/api/routes.py`** — `_require_project_key` falls back to `load_delivery_automation_config().project_key` when no header/body key.
2. **`scripts/cloud-preflight.sh`** — credential + Hermes gate preflight (no secret logging).
3. **`scripts/cloud-workflow-frt.sh`** — scripted Hermes-mounted FRT driver for phases A–D.
4. **`docs/cursor-cloud/bootstrap.md`** — document preflight and workflow FRT scripts.

## Next Agent Instructions

1. **Configure Automation secrets** on the Cursor Cloud automation (`JIRA_*`, `GITHUB_TOKEN`, `GH_TOKEN`, `STRIPE_SECRET_KEY`).
2. Re-run: `./scripts/cloud-bootstrap.sh && ./scripts/cloud-preflight.sh && ./scripts/cloud-workflow-frt.sh`.
3. Complete phases E–H manually or extend `cloud-workflow-frt.sh` with gate-approval loop.
4. Fix vitest TZ assertions (`use-daily-analysis.test.ts`) to use UTC or locale-agnostic formatting.
5. Investigate `test_promote_ready_record` expecting `awaiting_gate` vs current `intake` status.

## Appendix

### Commands run

```bash
./scripts/cloud-bootstrap.sh
./scripts/cloud-preflight.sh   # exit 1 — 4 credential blockers
./scripts/cloud-workflow-frt.sh
curl -H "X-Hermes-Session-Token: cloud-agent-session-token" \
  -H "X-LC-Project-Key: TVP" \
  http://127.0.0.1:9119/api/plugins/livingcolor/delivery/overview
```

### Credential scan (names only)

All required sandbox credentials: **missing** from process environment.

### Security note

Credentials listed in automation prompt templates are not automatically injected into the Cloud Agent VM. Configure them in the Automation secrets UI; never commit tokens to the public repository.
